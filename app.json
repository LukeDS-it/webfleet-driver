{
  "name": "webfleet-driver",
  "scripts": {
    "postdeploy": "psql -d $DATABASE_URL -a -f scripts/database.sql"
  },
  "env": {
    "SERVER_PORT": {
      "value": "$PORT"
    }
  },
  "formation": {
    "web": {
      "quantity": 1
    }
  },
  "addons": [
    "heroku-postgresql"
  ],
  "buildpacks": [
    {
      "url": "heroku/scala"
    }
  ]
}
